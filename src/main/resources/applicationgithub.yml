spring:
  datasource:
    url:
    username:
    password:
    driver:
      class-name:
  jpa:
    database-platform:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
server:
  error:
    include-stacktrace: never
